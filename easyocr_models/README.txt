Legacy folder — EasyOCR was removed from Kathana Helper.

Mob names are matched with OpenCV template matching on the enemy name/level bar (see mob_filter.py).
This directory is kept for compatibility with older build scripts and can be deleted safely.
